# backend/.env

# LiveKit Cloud Credentials
LIVEKIT_URL=wss://interviewer-ejx32g9n.livekit.cloud
LIVEKIT_API_KEY=APIEXLKswqU2ea5
LIVEKIT_API_SECRET=iVQifpwxfdId0XejxZkkHuS6fG75VYr0wr12foR82BkD

# Backend Server Port
PORT=8080
# Frontend URL (for CORS) - Adjust if your frontend runs elsewhere
FRONTEND_URL=http://localhost:3000
REACT_APP_BACKEND_URL=http://localhost:8080

# Frontend URL (for CORS) - Adjust if your frontend runs elsewhere
FRONTEND_URL=http://localhost:3000

# --- NEW: Agent Worker Configuration ---
# URL where the Python agent worker is listening for job requests
AGENT_WORKER_URL=http://localhost:8090 # Ensure port matches agent/.env AGENT_WORKER_PORT

# Identity the backend expects the agent to use (optional, for logging/reference)
AGENT_IDENTITY=ai-interviewer # Matches default in agent/.env